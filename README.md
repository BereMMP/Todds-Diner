# Todds-Diner

This is a program made in Godot that acts as a recipe application. The main objective of this application is to allow users to create and read recipes with characters that derive from the DC franchise. 

Currently the base design for the program has been made. This is a WIP, with the next step being to implement the function to create and read JSON files, which stores the recipes.
